# NT Copper Prospectivity Mapping — Northern Territory (Cu)

End-to-end geospatial ML project: curate multi-source geoscience data → feature engineering → PU learning + uncertainty → 2D targeting map → XAI (SHAP) → software engineering (tests/CI).

See `configs/base.yaml` and run via `python -m nt_cu_prospectivity.cli ...`.
